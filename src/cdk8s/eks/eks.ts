import { Construct } from "constructs";

export interface AksChartsProps {}

export class EksCharts extends Construct {
  private readonly;

  constructor(scope: Construct, name: string, props: EksChartsProps) {
    super(scope, name);
  }
}
