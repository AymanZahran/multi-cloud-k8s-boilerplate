import { Construct } from "constructs";

export interface AksChartsProps {}

export class AksCharts extends Construct {
  private readonly;

  constructor(scope: Construct, name: string, props: AksChartsProps) {
    super(scope, name);
  }
}
