# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: cdktf-dev-deploy
on:
  push:
    branches:
      - master
jobs:
  build:
    name: cdktf-dev-deploy
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      TF_API_TOKEN: ${{ secrets.TF_API_TOKEN }}
      AWS_ACCESS_KEY_ID: AKIA5EZUTDHMR45HQYKA
      AWS_SECRET_ACCESS_KEY: ${{ secrets.dev_AWS_SECRET_ACCESS_KEY }}
      ARM_CLIENT_ID: ff9f2cd0-a91d-4f34-97ac-6670bb08cc05
      ARM_CLIENT_SECRET: ${{ secrets.dev_AZURE_CLIENT_SECRET }}
      ARM_TENANT_ID: 3e5e67ad-2e8c-4a0a-b170-8fe996eebec0
      ARM_SUBSCRIPTION_ID: 7feba40e-3797-4714-bffd-788ab08a6cde
      stack: dev
      context: deploy
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Use Node.js 19.x
        uses: actions/setup-node@v3
        with:
          node-version: 19.x
          cache: npm
      - name: Install cdktf-cli v0.17.0
        run: npm install -g cdktf-cli@0.17.0
      - name: Install Terraform v1.5.3
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.3
      - name: Install dependencies
        run: npx projen install:ci
      - name: Install Terraform Providers and Modules
        run: npx projen cdktf-get
      - name: Set Terraform Token
        run: |-
          mkdir -p ~/.terraform.d
          echo '{
            "credentials": {
              "app.terraform.io": {
                "token": "${{ secrets.TF_API_TOKEN }}"
              }
            }
          }' > ~/.terraform.d/credentials.tfrc.json
      - name: AZ Login
        run: |-
          az login --service-principal -u ff9f2cd0-a91d-4f34-97ac-6670bb08cc05 -p ${{ secrets.dev_AZURE_CLIENT_SECRET }} --tenant 3e5e67ad-2e8c-4a0a-b170-8fe996eebec0
          az account set --subscription 7feba40e-3797-4714-bffd-788ab08a6cde
      - name: Terraform Plan
        run: if [ "${{ env.context }}" == "build" ]; then cdktf plan ${{ env.stack }}; fi
      - name: Comment on the PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@0.9.0
        env:
          PLAN: terraform\n${{ steps.plan.outputs.stdout }}
        with:
          github-token: ${{ secrets.GH_COMMENT_TOKEN }}
          script: |-
            const output = `#### Terraform Plan 📖\`${{ steps.plan.outcome }}\`
            <details><summary>Show Plan</summary>
            \`\`\`${process.env.PLAN}\`\`\`
            </details>
            *Pusher: @${{ github.actor }}, Action: \`${{ github.event_name }}\`, Working Directory: \`${{ env.tf_actions_working_dir }}\`, Workflow: \`${{ github.workflow }}\`*`;
            github.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })
